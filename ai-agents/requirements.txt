# ─── Web Framework ───────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-dotenv>=1.0.0
pydantic>=2.7.0

# ─── NATS Messaging ──────────────────────────────────────────────────────────
nats-py>=2.7.2
aiohttp>=3.9.5

# ─── LangChain Core ──────────────────────────────────────────────────────────
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-text-splitters>=0.2.0

# ─── LLM Providers (Auto-selected by rag_query.py based on env vars) ─────────
langchain-openai>=0.1.0          # Uses OPENAI_API_KEY
langchain-google-genai>=1.0.0    # Uses GOOGLE_API_KEY (FREE tier available!)

# ─── LangGraph AI Orchestrator ───────────────────────────────────────────────
langgraph>=0.1.0

# ─── Embeddings (Local — no API key needed) ──────────────────────────────────
langchain-huggingface>=0.0.3
sentence-transformers>=2.7.0

# ─── Vector Database (ChromaDB — local) ──────────────────────────────────────
langchain-chroma>=0.1.2
chromadb>=0.5.0

# ─── Document Loaders ────────────────────────────────────────────────────────
pymupdf>=1.24.0      # PDF loader
docx2txt>=0.8        # DOCX loader
